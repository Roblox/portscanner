[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "portscanner-parser"
version = "0.1.0"
description = "Generation-safe ACT scan result parser"
requires-python = ">=3.12"
dependencies = [
    "boto3",
    "defusedxml",
    "portscanner-contracts",
    "psycopg[binary]",
]

[project.optional-dependencies]
test = ["pytest"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --import-mode=importlib"
markers = [
    "integration: requires TEST_DATABASE_URL",
]
